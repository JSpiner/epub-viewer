<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.jspiner.viewer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="net.jspiner.viewer.ui.library.LibraryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="net.jspiner.viewer.ui.reader.ReaderActivity" android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name="net.jspiner.viewer.ui.etc.EtcActivity" />
        <activity android:name="net.jspiner.viewer.ui.search.SearchActivity" android:theme="@style/AppTheme.Transparent" />
    </application>

</manifest>